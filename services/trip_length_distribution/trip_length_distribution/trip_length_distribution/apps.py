from django.apps import AppConfig

class TripLengthDistributionConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "services.trip_length_distribution"
    verbose_name = "Trip Length Distribution"
